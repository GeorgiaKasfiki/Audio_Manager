package grAudioManager.app;

public enum Choice {
    ADD,
    REMOVE,
    DISPLAY,
    CLEAR,
    TOTALCOST,
    TOTALSIZE,
    TOTALDURATION,
    AVGDURATION,
    SAVE,
    LOAD,
    TOTALRECORDS,
    FIND,
    EXIT,
    ERROR;
}